##
 # @package Barasaki Semesta
 # @copyright Barasaki Semesta
 # @author Sikelopes
 # @version 1.0
 # @access Public
 # @path /barasaki-btm/.htaccess
 ##
 
<IfModule mod_rewrite.c>
	RewriteEngine On

	RewriteCond $1 !^(index\.php)
	RewriteRule ^(.*)$ index.php/$1 [L]
</IfModule>